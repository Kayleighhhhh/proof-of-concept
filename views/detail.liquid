{% render 'partials/head.liquid' %}

<main>
  <article>
    <h2>
      <a href="{{ item.link }}">TITEL TOPIC</a>
    </h2>
    <p>{{ item.name }}</p>
    <p>{{ item.preview }}</p>
  </article>
</main>
{% render 'partials/foot.liquid' %}
